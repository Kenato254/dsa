///Durstenfeld shuffle: 

/// This is a variant of the Fisher-Yates shuffle with a slightly different implementation, 
/// sometimes preferred for its simplicity. It achieves the same result but uses 
/// a different approach for selecting the random element.
pub fn durstenfeld_shuffle(arr: &mut Vec<i32>) {
    todo!()
}